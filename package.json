{
  "name": "react-pdv",
  "version": "0.0.7",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development kit examples --mode library",
    "build": "babel ./src -d dist",
    "build:dev": "cross-env NODE_ENV=development yarn build --watch",
    "build:prod": "cross-env NODE_ENV=production yarn build",
    "prepublish": "npm -s run build:prod",
    "prettify": "prettier --write **/*.js !**/dist/**",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vicentedealencar/react-pdv.git"
  },
  "babel": {
    "presets": [
      "react-app"
    ]
  },
  "prettier": {
    "proseWrap": "never",
    "singleQuote": true,
    "semi": false
  },
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {
    "@compositor/kit-cli": "^1.0.37",
    "@compositor/kit-snapshot": "^1.0.24",
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.1",
    "cross-env": "^5.1.6",
    "jest": "^23.1.0",
    "prettier": "^1.13.4",
    "react": "^16.4.0"
  },
  "dependencies": {
    "invariant": "^2.2.4"
  }
}
